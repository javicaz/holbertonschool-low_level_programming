#include "main.h"

/**
 * print_alphabet- the alphabet will show
 * description- it will print the alphabet
 * Return: 0
 */
void print_alphabet(void)
{
	int l;

	for (l = 'a'; l <= 'z'; l++)
	{
		_putchar(l);
	}
	_putchar('\n');
}

